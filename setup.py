from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Train neural networks to classify traffic signs',
    author='Djole',
    license='MIT',
)
